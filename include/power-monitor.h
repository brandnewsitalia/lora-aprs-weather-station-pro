#ifndef POWER_MONITOR_H\n#define POWER_MONITOR_H\n\n#include <Arduino.h>\n\nclass PowerMonitor {\npublic:  \n    PowerMonitor(int pin);\n    float readVoltage();\n    float calculatePercentage(float voltage);\n    bool isLowBattery(float percentage);\n\nprivate:  \n    int pin;\n    const float MAX_VOLTAGE = 4.2; // Example max voltage for Li-ion\n    const float MIN_VOLTAGE = 3.0; // Example min voltage for warning\n    const float LOW_BATTERY_THRESHOLD = 20.0; // Percentage\n};\n\n#endif // POWER_MONITOR_H